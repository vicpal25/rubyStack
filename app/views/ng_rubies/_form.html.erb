<%= form_for(@ng_ruby) do |f| %>
  <% if @ng_ruby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ng_ruby.errors.count, "error") %> prohibited this ng_ruby from being saved:</h2>

      <ul>
      <% @ng_ruby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
